base_path <- "../daten-generator/daten/"

abschlüsse_path <- paste0(base_path, "Abschlüsse.xlsx")
klassen_path <- paste0(base_path, "Klassen.xlsx")
schulen_path <- paste0(base_path, "Schulen.xlsx")
schueler_path <- paste0(base_path, "Schüler.xlsx")
unterrichte_path <- paste0(base_path, "Unterrichte.xlsx")

wk_code <- "644000000000"